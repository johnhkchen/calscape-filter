import { describe, it, expect } from 'vitest';

describe('sum test', () => {
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});

describe('subtract test', () => {
	it('adds 4 - 3 to equal 1', () => {
		expect(4 - 3).toBe(1);
	});
});
